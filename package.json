{
  "name": "fulltank-mock-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
